mutation SetRating(
  $code: Int!
  $media: String!
  $rating: Float!
  $seasonNumber: Int
  $episodeNumber: Int
) {
  rating(
    code: $code
    media: $media
    rating: $rating
    seasonNumber: $seasonNumber
    episodeNumber: $episodeNumber
  ) {
    status
  }
}
