mutation SetFavorite(
  $code: Int!
  $type: String!
  $favorite: Boolean!
  $seasonNumber: Int
  $episodeNumber: Int
) {
  favorite(
    code: $code
    type: $type
    favorite: $favorite
    seasonNumber: $seasonNumber
    episodeNumber: $episodeNumber
  ) {
    status
  }
}
