mutation SetFovorite(
  $code: Int!
  $media: String!
  $favorite: Boolean!
  $seasonNumber: Int
  $episodeNumber: Int
) {
  favorite(
    code: $code
    media: $media
    favorite: $favorite
    seasonNumber: $seasonNumber
    episodeNumber: $episodeNumber
  ) {
    status
  }
}
