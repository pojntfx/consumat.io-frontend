#import "../fragments/MovieFields.graphql"
#import "../fragments/TvFields.graphql"

query GetList($type: String!, $watchStatus: String!) {
  list(type: $type, watchStatus: $watchStatus) {
    ... on Movie {
      ...MovieFields
    }
    ... on TV {
      ...TvFields
    }
  }
}
